namespace VketTools.Validator.RuleSets
{
    public class Vket2025SummerOfficialAssetData : VketUdonOfficialAssetData
    {
        // NOTE: 追加する必要があれば適宣オーバーライド
        /*protected override string[] GetGUIDs()
        {
            var guids = base.GetGUIDs().ToList();
            var addGuids = new string[]
            {
            };
            guids.AddRange(addGuids);
            return guids.ToArray();
        }
        */
    }
}